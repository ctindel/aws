#!/bin/bash

# These get created as the ubuntu user by the file provisioner but 
#  beats will flip out if they're not owned by root
#chown root:root /etc/metricbeat/metricbeat.yml
#chown root:root /etc/packetbeat/packetbeat.yml
